# Linting Jobs - Prettier & ESLint
# Laufen parallel in der lint stage

prettier:
  extends: .base-config
  stage: lint
  script:
    - echo "🎨 Running Prettier code formatting check..."
    - pnpm prettier --check .
  allow_failure: false

eslint:
  extends: .base-config
  stage: lint
  script:
    - echo "🔍 Running ESLint code quality check..."
    - pnpm lint
  allow_failure: false
